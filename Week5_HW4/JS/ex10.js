// let originalWord = "Bora-Bora";
const originalWord = "Bora-Bora";


const lowercaseWord = originalWord.toLowerCase();
console.log(lowercaseWord); // "bora-bora"

const uppercaseWord = originalWord.toUpperCase();
console.log(uppercaseWord); // "BORA-BORA"

originalWord.toLowerCase(); // Fixed error on hands-on tutorial

console.log(originalWord);

// Experiment below
// I could reassign the variable originalWord to the lowerCase value.  Must change const to let on line 1

// originalWord = originalWord.toLowerCase();
// console.log(originalWord);
